{
    "name": "@faas/registry",
    "version": "1.0.0",
    "description": "",
    "scripts": {
      "start": "deno run --allow-net --allow-read=./public,./shared server.ts",
      "example": "export AUTH_TOKEN=my_auth && deno run --allow-net --allow-env --allow-read ./example/users-search/main.ts"
    }
}